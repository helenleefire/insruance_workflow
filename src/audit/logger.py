"""Audit logger — records input, extraction, proposal, override, and final verdict
per claim, timestamped, so every decision is traceable back to its reasoning."""

# TODO: implement once GraphState fields are populated end-to-end


def log_decision(state):
    raise NotImplementedError
