"""Node: flatten_for_decision
Reads: state.extraction
Does: plain Python transform — no LLM call — reshapes ClaimExtraction into DecisionInput
Returns: {"decision_input": DecisionInput(...)}
"""

# from src.schemas.claim_schema import DecisionInput, GraphState


def flatten_for_decision(state):
    raise NotImplementedError
